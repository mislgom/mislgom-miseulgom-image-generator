# JWT Secret Key
# Used for signing and verifying JWT tokens
# Generate a strong random string (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-to-something-secure

# Encryption Key for API Keys
# Used for encrypting user API keys in the database
# Generate a strong random string (minimum 32 characters)
# Example: openssl rand -base64 32
ENCRYPTION_KEY=your-encryption-key-change-this-to-something-secure

# Vercel KV (Redis) Configuration
# These are automatically provided by Vercel when you add a KV database
# You don't need to set these manually - they're set by Vercel
# KV_REST_API_URL=https://your-kv-instance.kv.vercel-storage.com
# KV_REST_API_TOKEN=your-kv-token

# Development Notes:
# 1. Copy this file to .env.local for local development
# 2. Never commit .env.local to git (it's in .gitignore)
# 3. Set these environment variables in Vercel dashboard for production
# 4. JWT_SECRET and ENCRYPTION_KEY should be different values
# 5. Use strong, randomly generated keys for production
